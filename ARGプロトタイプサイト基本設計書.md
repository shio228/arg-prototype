# ARGプロトタイプサイト基本設計書

## 1. システム全体構成

ARGプロトタイプサイトは、単一のウェブサイト内で完結する物語体験を提供します。以下の主要コンポーネントで構成されます。

### 1.1 システムアーキテクチャ

本プロトタイプは、React 19 + Tailwind CSS 4を基盤とした静的フロントエンドアプリケーションとして実装されます。バックエンド処理は最小限とし、クライアント側でのデータ管理と状態管理を中心に設計します。

| コンポーネント | 役割 | 技術スタック |
|---|---|---|
| **フロントエンド** | ユーザーインターフェース、物語表示、謎解き機能 | React 19, Tailwind CSS 4, shadcn/ui |
| **ページ管理** | ルーティング、ページ遷移制御 | Wouter（クライアント側ルーター） |
| **状態管理** | ゲーム進行状況、ユーザー入力の保持 | React Context API |
| **メディア処理** | 画像、動画、音声の表示・再生 | HTML5 Media Elements |

### 1.2 データフロー

```
ユーザー操作 → イベントハンドラ → 状態更新 → ページ再レンダリング → UI更新
```

ユーザーが謎の答えを入力したり、ページをクリックしたりすると、その操作がイベントハンドラで捕捉され、アプリケーション状態が更新されます。状態の変更に基づいて、適切なページコンポーネントが再レンダリングされ、新しいコンテンツが表示されます。

## 2. 主要機能の設計

### 2.1 ストーリー表示機能

**目的**: 物語の断片、背景情報、ヒントをユーザーに提示する。

**実装方法**:
- 各ページコンポーネント（`pages/`ディレクトリ）が、固有の物語内容、ヒント、メディア要素を含む。
- ページ内容はJavaScript オブジェクトとして定義され、コンポーネント内でレンダリングされる。
- 物語の進行に応じて、異なるページが表示される。

**主要なページ構成**:
- **ホームページ**: ARGの導入、物語の背景説明、謎解き入力フォーム
- **ストーリーページ**: 物語の各段階を表示するページ、謎をちりばめた形で配置、謎解き入力フォーム
- **隠しページ**: パスワードやヒント、特定のワードからのみアクセス可能なページ、謎解き入力フォーム

### 2.2 謎解き・インタラクション機能

**目的**: ユーザーが謎を解き、物語を進行させる。

**実装方法**:
- **入力フォーム**: 全ページに配置された shadcn/ui の `Input` コンポーネントを使用して、ユーザー入力を受け取る。
- **謎の配置**: 各ストーリーページの物語テキスト、メディア、ヒントの中に謎をちりばめ、ユーザーが謎を発見・解く必要がある。
- **正誤判定**: 入力値と正解をクライアント側で比較し、正誤を判定する。
- **条件付きページ遷移**: 謎を正しく解くと、フィードバックが表示され、必要に応じて次のページへのリンクが表示される。
- **隠しページへのアクセス**: 特定のワードを入力フォームに入力することで、隠しページへのリンクが表示される。

**謎解きのロジック**:
```
入力値の取得 → 大文字小文字の統一 → 正解との比較（空白は区切り文字として使用） → 結果表示（フィードバック、リンク表示、隠しページアクセス判定）
```

### 2.3 ページ遷移機能

**目的**: ユーザーの操作に基づいて、適切なページへ遷移させる。

**実装方法**:
- **通常のナビゲーション**: ページ内のリンクをクリックして遷移する。
- **条件付き遷移**: 謎を解く、パスワードを入力するなど、特定の条件を満たすことで次のページへ遷移できる。
- **隠しページ**: URLを直接入力するか、ヒントから得られるリンクからのみアクセス可能。

**ルーティング設計**:
- `/` - ホームページ
- `/story/1` - ストーリーページ1（謎解き入力フォーム、謎を含む）
- `/story/2` - ストーリーページ2（謎解き入力フォーム、謎を含む）
- `/story/3` - ストーリーページ3（謎解き入力フォーム、謎を含む）
- `/hidden/secret` - 隠しページ（特定のワード入力で到達、謎解き入力フォーム）

### 2.4 メディア表示機能

**目的**: 画像、動画、音声などのメディア要素を物語に組み込む。

**実装方法**:
- **画像**: `<img>` タグまたは `next/image` 相当の最適化コンポーネントを使用。
- **動画**: HTML5 `<video>` タグを使用し、ブラウザネイティブのプレイヤーを提供。
- **音声**: HTML5 `<audio>` タグを使用し、音声ファイルを再生。

**メディア管理**:
- すべてのメディアファイルは、CDN（`manus-upload-file --webdev`で取得）にホストされ、URLで参照される。
- ローカルファイルはプロジェクトディレクトリに保存しない（デプロイ時のタイムアウト防止）。

## 3. ページ構造とコンポーネント設計

### 3.1 ページ構成図

```
App.tsx (ルーター)
├── Home (ホームページ)
├── StoryPage1 (ストーリーページ1)
├── StoryPage2 (ストーリーページ2)
├── StoryPage3 (ストーリーページ3)
├── HiddenPage (隠しページ)
└── NotFound (404ページ)
```

### 3.2 主要コンポーネント

| コンポーネント | 場所 | 役割 |
|---|---|---|
| **App** | `App.tsx` | ルーター、トップレベルレイアウト |
| **Home** | `pages/Home.tsx` | ホームページ、謎解き入力フォーム |
| **StoryPage** | `pages/StoryPage.tsx` | ストーリー表示、謎の配置、謎解き入力フォーム |
| **HiddenPage** | `pages/HiddenPage.tsx` | 隠しページ、謎解き入力フォーム |
| **Header** | `components/Header.tsx` | ヘッダーナビゲーション |
| **Footer** | `components/Footer.tsx` | フッター |
| **PuzzleForm** | `components/PuzzleForm.tsx` | 謎解き入力フォーム（全ページで再利用） |
| **MediaDisplay** | `components/MediaDisplay.tsx` | メディア表示 |

### 3.3 コンポーネント間のデータフロー

```
App
├── Header
│   └── ナビゲーションリンク
├── [ページコンポーネント]
│   ├── ストーリーテキスト
│   ├── 謎（ストーリー内に埋め込まれた形）
│   ├── MediaDisplay
│   │   ├── 画像
│   │   ├── 動画
│   │   └── 音声
│   ├── PuzzleForm（全ページに配置）
│   │   ├── 入力フィールド
│   │   ├── 送信ボタン
│   │   ├── 結果メッセージ
│   │   ├── フィードバック
│   │   └── 隠しページアクセスリンク（特定ワード入力時）
│   └── ナビゲーションリンク
└── Footer
    └── フッターテキスト
```

## 4. 状態管理設計

### 4.1 グローバル状態

React Context APIを使用して、以下のグローバル状態を管理します。

| 状態 | 型 | 説明 |
|---|---|---|
| `gameProgress` | Object | ゲーム進行状況（解いた謎、訪問したページなど） |
| `solvedPuzzles` | Array | 解いた謎のID |
| `unlockedPages` | Array | アクセス可能なページのID |
| `playerInput` | Object | プレイヤーの入力履歴 |

### 4.2 ローカル状態

各ページコンポーネント内で管理される状態：

| 状態 | 型 | 説明 |
|---|---|---|
| `inputValue` | String | 入力フォームの現在の値 |
| `isCorrect` | Boolean | 謎の答えが正しいか |
| `showHint` | Boolean | ヒントを表示するか |
| `mediaPlaying` | Boolean | メディアが再生中か |

## 5. ページ遷移ロジック

### 5.1 条件付きページ遷移の実装

```javascript
// 例：謎を解いた後の遷移
if (userAnswer === correctAnswer) {
  // グローバル状態を更新
  updateGameProgress({ solvedPuzzles: [...solvedPuzzles, puzzleId] });
  // 次のページへのリンクを表示
  showNextPageLink();
}
```

### 5.2 隠しページへのアクセス制御

隠しページは、以下の方法でのみアクセス可能：

1. **特定のワード入力**: 各ページの謎解き入力フォームに特定のワードを入力することで、隠しページへのリンクが表示される。
2. **ヒントから得られるURL**: ストーリーページ内に隠されたテキストやメディアから、隠しページのURLを推測する。

**隠しページアクセスのロジック**:
```
入力値の取得 → 大文字小文字の統一 → 隠しページキーワードとの比較（空白は区切り文字として使用） → マッチした場合、隠しページへのリンクを表示
```

## 6. 設計上の考慮事項

### 6.1 スケーラビリティ

- ページ情報は、JSON形式で外部ファイルに保存し、必要に応じて読み込む設計を検討。
- 謎の正解情報も、設定ファイルで管理できるようにする。

### 6.2 セキュリティ

- クライアント側で謎の正解を保持するため、ブラウザのDevToolsで確認可能。プロトタイプのため、高度なセキュリティ対策は不要だが、正解情報を難読化することは検討する。



## 7. 今後の詳細設計

次のフェーズ（詳細設計）では、以下の項目を詳細に設計します：

1. **各ページの具体的なコンテンツ**: 物語のテキスト、謎の内容、メディアファイルのURL。
2. **謎の設計**: 各ストーリーページに配置される謎の詳細、正解、ヒント、複雑な判定ロジック。
3. **隠しページキーワード**: 隠しページへのアクセスに必要な特定のワード。
4. **コンポーネントの詳細実装**: 各コンポーネントのprops、状態、イベントハンドラ。
5. **スタイリング設計**: Tailwind CSS設定、色パレット、タイポグラフィ。

以上
